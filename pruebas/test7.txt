programa COVID19;
var
int: j, x, w;

funcion int fibonacci(int r)
var
int : a, b, c, contador;
{
    contador = 0;
    a = 0;
    b = 1;
    mientras (contador <= r) haz {
        escribe(a);
        c = a + b;
        a = b;
        b = c;
        contador = contador + 1;
    }
    regresa(a);
}

funcion int reFibonacci(int n)
{
    si (n == 0) entonces {
        regresa(0);
    }
    si (n == 1) entonces{
        regresa(1);
    }
    si (n > 1) entonces {
        regresa(reFibonacci(n-1) + reFibonacci(n-2));
    }
}

principal()
{
    lee(j);
    x = fibonacci(j);
    w = reFibonacci(j);
    escribe(x);
    escribe(w);
}